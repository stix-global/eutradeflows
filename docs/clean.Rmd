---
output:
  pdf_document: 
    fig_caption: yes
    toc: yes
  html_document: default
---
```{r databasetableanalysed, echo=FALSE}
# Comment out to pass tableanalysed as a parameter in the parent environment
tableanalysed = 'raw_comext_monthly_201708' 
con <- RMySQL::dbConnect(RMySQL::MySQL(), dbname = "tradeflows")
```
---
title: "Clean the database table `r tableanalysed`"
---

```{r setup, message=FALSE, warning=FALSE, echo=FALSE}
library(knitr)
opts_knit$set(root.dir="..") # file paths are relative to the root of the project directory
opts_chunk$set(echo = FALSE)
library(eutradeflows)
library(dplyr)
library(tidyr)
library(magrittr)
library(ggplot2)
``` 


See /tradeflows/docs/development/cleaning/Comext.Rmd
In that document, tradeflows functions (originally written for Comtrade) 
are tested on the Comext data.


# Introduction
```{r echo = FALSE}
library(tradeflows)
wp <- tbl(con, tableanalysed) %>% 
    filter(productcode == 44)

clean(wp)

```
    
